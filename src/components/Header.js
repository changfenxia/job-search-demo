import React from 'react';
import Progress from './Progress';

const Header = () => (
    <div>
        <h1>Best jobs for digital marketing professionals</h1>
        <Progress />
    </div>
);

export default Header;